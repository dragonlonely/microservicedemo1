server:
  port: 12900
spring: #配置文件的仓库地址
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/jackdragon/spring-cloud-config #默认配置
          search-paths:
            - abc #以abc 开头的 会转到abc目录
            - def
          clone-on-start: true
          #username:  #需要用户名密码的情况下设置
          #password: